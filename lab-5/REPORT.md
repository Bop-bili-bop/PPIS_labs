# Лабораторна робота №5

## Тема

Використання генеративного ШІ при розробці Python-застосунків, парсингу
веб-сторінок та аналізі даних.

## Мета

Опанувати створення невеликих Python-застосунків за підтримки генеративного ШІ,
навчитися отримувати структуровані дані з HTML, аналізувати CSV та представляти
результати у Flask-застосунку.

## Парсинг веб-сторінок

`requests` виконує HTTP(S)-запит з User-Agent і тайм-аутом, а
`raise_for_status()` перетворює помилковий HTTP-статус на виняток.
`BeautifulSoup` розбирає HTML, після чого селектор `h1, h2, h3` повертає рівень
і очищений текст кожного заголовка. Валідується схема та наявність домену;
мережеві помилки перетворюються на контрольовану `ScraperError`. Парсер окремий
від завантаження, тому тести використовують статичний HTML без зовнішньої мережі.

## Аналіз даних

`pandas.read_csv()` створює `DataFrame` з навчального CSV. `analyze_csv()`
перевіряє існування файла й колонок, числовий тип колонки середнього, фільтрує
рядки за категорією та обчислює `mean()`. Порожня вибірка повертає зрозумілу
контрольовану помилку. Для категорії A відібрано 3 рядки, середня ціна — 526.67.

## Flask

`src/app.py` створює Flask application з маршрутами `GET /`, `POST /analyze` та
`POST /scrape`. Шаблон `templates/index.html` має форми аналізу й парсингу та
блок стану Gemini. Сервер запускається командою `python -m src.app` або
`flask --app src.app run --debug`. Тестовий клієнт підтвердив відповідь `GET /`
зі статусом 200 без Gemini-ключа.

## Gemini

Методика демонструє ChatGPT/OpenAI API, але для курсової реалізації провайдером
генеративного ШІ обрано Google Gemini. Lab 5 лише безпечно показує, чи
налаштовано `GEMINI_API_KEY`; аналіз і парсинг працюють незалежно. Повна
інтеграція офіційного SDK та чат знаходяться в Lab 4. Реальний ключ не
зберігається у Git.

## Результати роботи

Фактично запущено аналіз CSV, парсер публічного `example.com`, pytest і Flask
із перевіркою `GET /`. Вивід без секретів збережено в
`results/example-output.txt`. Gemini API integration implemented but was not
executed because GEMINI_API_KEY is intentionally not stored in the repository.

## Висновок

Реалізовано та перевірено безпечний веб-парсер, повторно використовуваний аналіз
CSV і невеликий Flask-інтерфейс. Автоматичні тести підтвердили основні та
помилкові сценарії без залежності від зовнішнього сайту або AI API.
