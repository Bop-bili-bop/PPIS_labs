# Лабораторна робота №4

## Тема

Програмування на Python за допомогою штучного інтелекту. Інтеграція Gemini API
та генерація алгоритмів.

## Мета роботи

Освоїти використання генеративного ШІ з Python, навчитися формувати точні
запити, а також створювати й автоматично перевіряти алгоритми.

## Використання Gemini замість ChatGPT

Методичні матеріали демонструють ChatGPT/OpenAI API. У практичній реалізації
використано альтернативний генеративний API Google Gemini та його офіційний
Python SDK `google-genai`. Залежності OpenAI відсутні.

## Опис API інтеграції

`gemini_client.py` завантажує локальний `.env` через `python-dotenv`, але також
підтримує системні змінні середовища. `GEMINI_API_KEY` є обов'язковим лише для
AI-запиту, а `GEMINI_MODEL` визначає модель. Функція `create_client()` створює
`genai.Client`; `generate_text()` передає текст у `models.generate_content` і
повертає текст відповіді. Помилки конфігурації, мережі та порожня відповідь
обробляються зрозумілими повідомленнями без розкриття ключа.

## Створення чат-бота

Консольний бот створює Gemini chat session, яка зберігає контекст поточного
діалогу. Цикл читає повідомлення, показує відповідь і завершується командами
`exit` або `quit`. Помилка окремого запиту не припиняє процес: користувач може
повторити спробу.

## Словесний опис алгоритму LCS

Алгоритм отримує два рядки та створює таблицю динамічного програмування для
всіх пар їхніх префіксів. Якщо поточні символи збігаються, до результату для
двох попередніх префіксів додається один. Інакше обирається найкращий результат
із сусіднього стану зверху або зліва. Після заповнення таблиці виконується
зворотний прохід від правого нижнього кута: однакові символи додаються до
відповіді, а в інших випадках перехід здійснюється до стану з більшою довжиною.
Отримані у зворотному порядку символи розвертаються.

## Реалізація

`lcs.py` окремо будує таблицю, повертає фактичну підпослідовність функцією
`longest_common_subsequence()` та її довжину функцією `lcs_length()`. CLI має
демонстраційні значення, але приймає довільні два рядки. Pytest охоплює
звичайні, однакові, різні, порожні та односимвольні рядки.

## Складність

Для довжин рядків `n` та `m` часова складність становить **O(n · m)**, а
просторова — **O(n · m)**.

## Результати

Фактично виконано приклад `ABCBDAB` / `BDCABA`: отримано LCS довжини 4.
Усі тести пройдено; точний журнал збережено у `results/example-output.txt`.
Gemini API integration implemented but was not executed because
GEMINI_API_KEY is intentionally not stored in the repository.

## Висновок

Реалізовано безпечне підключення Gemini, контекстний консольний чат і перевірений
алгоритм LCS. Практика продемонструвала формування запитів та обов'язкову
перевірку програмного результату автоматичними тестами.
